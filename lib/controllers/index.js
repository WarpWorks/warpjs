const app = require('./app');
const crud = require('./crud');
const domainDetails = require('./domain-details');
const portal = require('./portal');

module.exports = {
    app,
    crud,
    domainDetails,
    portal
};
