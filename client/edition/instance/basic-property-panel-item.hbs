<div class="form-group basic-property-pi warpjs-basic-property-{{name}} {{#if readOnly}}warpjs-readonly-field{{/if}}">
  <label class="col-xs-12">
    <span class="col-xs-2 control-label">{{label}}</span>
    <div class="col-xs-10">
      {{#if model.propertyTypeCheck.isBoolean}}
        <input
            class="form-control"
            type="checkbox"
            data-doc-level="{{model.docLevel}}"
            {{#if model._links.self}}data-warpjs-url="{{model._links.self.href}}"{{/if}}
            {{#if model.value}}checked{{/if}}
            {{#if readOnly}}readonly{{/if}}
            {{#unless canEdit}}disabled{{/unless}}
            />
      {{else if model.propertyTypeCheck.isFile}}
        {{>./../file-upload/button pull="pull-right" url=model._links.upload.href canEdit=canEdit}}

        <input class="form-control warpjs-file-field" type="text" value="{{model.value}}"
          data-doc-level="{{model.docLevel}}"
          {{#if model._links.self}}data-warpjs-url="{{model._links.self.href}}"{{/if}}
            {{#if readOnly}}readonly{{/if}}
          {{#unless canEdit}}disabled{{/unless}}
          />
      {{else}}
        {{#if (isPropertyTypeText)}}
          <span class="glyphicon glyphicon-list-alt pull-right"
              data-warpjs-action="open-wysiwyg"
              data-warpjs-can-edit="{{canEdit}}"
              ></span>

          <textarea
            class="form-control warpjs-text-field"
            type="{{model.inputType}}"
            readonly
            rows="7"
            data-doc-level="{{model.docLevel}}"
            data-warpjs-can-edit="{{canEdit}}"
            {{#if model._links.self}}data-warpjs-url="{{model._links.self.href}}"{{/if}}
            {{#if readOnly}}disabled{{/if}}
            {{#unless canEdit}}disabled{{/unless}}
            >{{model.value}}</textarea>
        {{else}}
          <input
            class="form-control"
            type="{{model.inputType}}"
            value="{{model.value}}"
            data-doc-level="{{model.docLevel}}"
            {{#if model._links.self}}data-warpjs-url="{{model._links.self.href}}"{{/if}}
            {{#if readOnly}}readonly{{/if}}
            {{#unless canEdit}}disabled{{/unless}}
            />
        {{/if}}
      {{/if}}
    </div>
  </label>
</div>
