<form class="form-horizontal">
    {{#if (isSeparator)}}
        {{> separator-panel-item}}
    {{else if (isBasicProperty)}}
        {{> basic-property-panel-item this instance=instance}}
    {{else if (isEnumPanelItem)}}
        {{> enum-panel-item this instance=instance}}
    {{else if (isRelationshipPanelItem)}}
        {{> relationship-panel-item this instance=instance}}
    {{else}}
        ***
        TODO
        ---
        name:{{name}}
        ---
        label:{{label}}
        ---
        type:{{type}} / {{style}}
        ***
    {{/if}}

</form>
